'use strict';

/**
 *  hall-descriptor controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::hall-descriptor.hall-descriptor');
